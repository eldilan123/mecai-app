# `supabase/` — Backend (configuración en HU-04)

> ⚠️ El contenido de estas carpetas se crea en **HU-04** (Setup de Supabase).
> En HU-03 solo queda la estructura.

| Carpeta       | Contenido                                                                      |
| ------------- | ------------------------------------------------------------------------------ |
| `migrations/` | SQL versionado: esquema, RLS, índices, seed data                               |
| `functions/`  | Edge Functions (Deno): `ai-assistant`, `calculate-schedules`, `send-reminders` |
