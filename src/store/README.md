# `src/store/` — Estado global (Zustand)

Stores pequeños y enfocados. Persistencia con MMKV donde aplique.

| Archivo            | Estado                      |
| ------------------ | --------------------------- |
| `auth.store.ts`    | Sesión y perfil del usuario |
| `vehicle.store.ts` | Vehículo activo             |
| `chat.store.ts`    | Conversaciones con la IA    |

Convención de nombres: `*.store.ts`.
