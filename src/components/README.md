# `src/components/` — Componentes reutilizables

| Carpeta        | Contenido                                                                      |
| -------------- | ------------------------------------------------------------------------------ |
| `ui/`          | Primitivos del design system: `Button`, `Card`, `Badge`, `Input`, `Typography` |
| `chat/`        | `ChatBubble`, `ChatInput`, `TypingIndicator`                                   |
| `vehicle/`     | `VehicleCard`, `StatusIndicator`                                               |
| `maintenance/` | `MaintenanceItem`, `UrgencyBadge`                                              |

Todos los estilos siguen el design system v2.0 (ver `src/constants/theme.ts` y
`tailwind.config.js`).
